# Python bytecode cache
__pycache__/
*.pyc

# Binary images — not built from source, stored externally or LFS
kernel/zImage
env/mtd3_env_prado_firmware_dump.bin
display/mtd4_arkdata_prado_firmware_dump.bin

# Rootfs build outputs (generated by build_rootfs.sh)
Prado firmware reconstructed/mtd6_rootfs/rootfs.ubifs
Prado firmware reconstructed/mtd6_rootfs/rootfs.img
Prado firmware reconstructed/mtd6_rootfs/ubi.cfg

# Userdata build outputs (generated by build_userdata.sh)
Prado firmware reconstructed/mtd7_userdata/userdata.ubifs
Prado firmware reconstructed/mtd7_userdata/userdata.img

# U-Boot env build output (generated by build_update.sh's env build step)
env/uboot-env.bin

# Rootfs runtime pseudo-directories (empty mountpoints, no content to track)
Prado firmware reconstructed/mtd6_rootfs/rootfs/proc/
Prado firmware reconstructed/mtd6_rootfs/rootfs/sys/
Prado firmware reconstructed/mtd6_rootfs/rootfs/dev/
Prado firmware reconstructed/mtd6_rootfs/rootfs/tmp/

# SD update output (generated by legacy/generate_update.sh — not for git)
sd_update/output/

# sd_bootable/ itself is now tracked (2026-07-29) -- a hand-placed asset in
# there (the real Toyota bootlogo.raw) was lost to an unreviewed overwrite
# while the whole directory was gitignored, with no way to recover it.
# Only the actual generated disk image is excluded below; everything else
# (bootlogo.raw, arkdata.ini, uEnv.txt, uboot binaries) is tracked so this
# can't happen silently again.
sd_bootable/sd_boot.img
# Pre-move (repo-root) location of the same output — kept ignored so any
# leftover local copy from before the sd_bootable/ move doesn't get staged.
/sd_boot.img

# uboot_sdboot.bin at repo root — was briefly tracked here, believed to be an
# ARK1680 BSP source-compiled U-Boot binary. Turned out to be corrupted (see
# corrupted/README.md and docs/UBOOT_SDBOOT_INVESTIGATION.md) and was moved to
# corrupted/. Ignored again here so a stray local copy (e.g. from re-running
# build_bootable_sdcard.sh) doesn't get accidentally re-staged.
/uboot_sdboot.bin
Holden firmware update/rootfs.img
firmware_dumps/CarSyncTech Toyota/CSTech-202511-IP17/rootfs.img
firmware_dumps/CarSyncTech Toyota/CSTech-202511-IP17/userdata.img
Holden firmware update/rootfs.img

# MCU firmware build outputs
hardware/MCU/source/build/
tools/mcu_builder/output/
